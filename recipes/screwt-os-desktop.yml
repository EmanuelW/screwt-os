---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: screwt-os-desktop
# description will be included in the image's metadata
description: ScrewtOS image for a Game/Code/Do-It-All computer

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/bazzite-dx
image-version: stable-44 # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - type: dnf
    repos:
      copr:
        - atim/starship
    install:
      packages:
        - micro
        - starship

  - type: dnf
    install:
      packages:
        - tmux
        - nextcloud-client

  - type: default-flatpaks
    configurations:
      - notify: true # Send notification after install/uninstall is finished (true/false)
        scope: system
        # If no repo information is specified, Flathub will be used by default
        install: # system flatpaks we want all users to have and not remove
          - org.gnome.Loupe
      - scope: user # Also add Flathub user repo, but no user packages

  - type: script
    snippets:
      # Ensure the CrealityPrint appimage work
      - "ln -s /lib64/libbz2.so.1 /lib64/libbz2.so.1.0"

  - type: systemd
    system:
      enabled:
        - podman.socket # U-Blue template does this by default. Investigate if it is needed

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
